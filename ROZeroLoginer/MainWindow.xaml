<Window x:Class="ROZeroLoginer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:utils="clr-namespace:ROZeroLoginer.Utils"
        Title="RO 帳號管理工具" MinHeight="500" MinWidth="800"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <utils:PrivacyConverter x:Key="PrivacyConverter"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="21*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,8" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal">
                <Button Name="AddAccountButton" Content="新增帳號" Style="{StaticResource PrimaryButtonStyle}" Click="AddAccountButton_Click"/>
                <Button Name="BatchAddButton" Content="批次新增" Style="{StaticResource PrimaryButtonStyle}" Click="BatchAddButton_Click"/>
                <Button Name="EditAccountButton" Content="編輯帳號" Style="{StaticResource SecondaryButtonStyle}" Click="EditAccountButton_Click" IsEnabled="False"/>
                <Button Name="BatchEditButton" Content="批次編輯" Style="{StaticResource SecondaryButtonStyle}" Click="BatchEditButton_Click"/>
                <Separator Margin="8,0"/>
                <Button Name="SettingsButton" Content="設定" Style="{StaticResource SecondaryButtonStyle}" Click="SettingsButton_Click"/>
                <Button Name="ViewLogButton" Content="查看日誌" Style="{StaticResource SecondaryButtonStyle}" Click="ViewLogButton_Click"/>
                <Button Name="CheckUpdateButton" Content="檢查更新" Style="{StaticResource SecondaryButtonStyle}" Click="CheckUpdateButton_Click"/>
                <Separator Margin="8,0"/>
                <Button Name="AboutButton" Content="關於" Style="{StaticResource SecondaryButtonStyle}" Click="AboutButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,57" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Account List -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 標題和按鈕 -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="帳號列表" FontSize="18" FontWeight="SemiBold" 
                                       Foreground="{StaticResource ForegroundBrush}" VerticalAlignment="Center"/>
                            <Button Name="ToggleVerticalTabButton" Content="垂直標籤" Style="{StaticResource SecondaryButtonStyle}" 
                                    Click="ToggleVerticalTabButton_Click" Width="75" Height="32" FontSize="12" Margin="12,0,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                                <Button Name="SelectAllButton" Content="全選" Style="{StaticResource SecondaryButtonStyle}" 
                                        Click="SelectAllButton_Click" Width="65" Height="32" FontSize="12"/>
                                <Button Name="SelectNoneButton" Content="全不選" Style="{StaticResource SecondaryButtonStyle}" 
                                        Click="SelectNoneButton_Click" Width="65" Height="32" FontSize="12" Margin="6,0,0,0"/>
                                <Button Name="LaunchSelectedButton" Content="啟動選中" Style="{StaticResource PrimaryButtonStyle}" 
                                        Click="LaunchSelectedButton_Click" Width="85" Height="32" FontSize="12" Margin="10,0,0,0" IsEnabled="False"/>
                                <Button Name="DeleteSelectedButton" Content="批次刪除" Style="{StaticResource DestructiveButtonStyle}" 
                                        Click="DeleteSelectedButton_Click" Width="85" Height="32" FontSize="12" Margin="6,0,0,0" IsEnabled="False"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 分組TAB區域 - 支援水平和垂直兩種模式 -->
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 水平模式的TabControl -->
                            <TabControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                                       Name="GroupTabControl" SelectionChanged="GroupTabControl_SelectionChanged"
                                       Background="Transparent" BorderThickness="0" Padding="0"
                                       TabStripPlacement="Top">
                            <TabControl.Resources>
                                <!-- 水平標籤樣式 (預設) -->
                                <Style TargetType="TabItem" x:Key="HorizontalTabStyle">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TabItem">
                                                <Border Name="Border" Background="Transparent" BorderThickness="0,0,0,2" 
                                                        BorderBrush="Transparent" CornerRadius="6,6,0,0" Margin="0,0,4,0" 
                                                        Padding="16,8,16,10">
                                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" 
                                                                      HorizontalAlignment="Center" ContentSource="Header" 
                                                                      TextBlock.FontSize="13" TextBlock.FontWeight="Medium"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="ContentSite" Property="TextBlock.FontWeight" Value="SemiBold"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="False">
                                                        <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="{StaticResource MutedForegroundBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                                
                                <!-- 垂直標籤樣式 -->
                                <Style TargetType="TabItem" x:Key="VerticalTabStyle">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TabItem">
                                                <Border Name="Border" Background="Transparent" BorderThickness="0,0,2,0" 
                                                        BorderBrush="Transparent" CornerRadius="6,0,0,6" Margin="0,0,0,4" 
                                                        Padding="12,16,10,16" MinWidth="120">
                                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" 
                                                                      HorizontalAlignment="Left" ContentSource="Header" 
                                                                      TextBlock.FontSize="13" TextBlock.FontWeight="Medium"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="ContentSite" Property="TextBlock.FontWeight" Value="SemiBold"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="False">
                                                        <Setter TargetName="ContentSite" Property="TextBlock.Foreground" Value="{StaticResource MutedForegroundBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TabControl.Resources>
                            </TabControl>
                            
                            <!-- 垂直模式的按鈕列表 -->
                            <Border Grid.Row="1" Grid.Column="0" 
                                   Name="VerticalTabBorder" Visibility="Collapsed"
                                   Width="120" Height="300" VerticalAlignment="Top" 
                                   Background="Transparent" Margin="0,0,8,0" BorderThickness="0">
                                <ScrollViewer Name="VerticalTabScrollViewer"
                                             VerticalScrollBarVisibility="Auto" 
                                             HorizontalScrollBarVisibility="Disabled"
                                             CanContentScroll="False">
                                    <ScrollViewer.Resources>
                                        <!-- 自定義滾動條樣式 -->
                                        <Style x:Key="ModernScrollBarThumb" TargetType="Thumb">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border Background="#C0C0C0" CornerRadius="3" Margin="2"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#808080"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        
                                        <Style TargetType="ScrollBar">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ScrollBar">
                                                        <Border Background="#F5F5F5" CornerRadius="4" Width="8">
                                                            <Track Name="PART_Track" IsDirectionReversed="True">
                                                                <Track.Thumb>
                                                                    <Thumb Style="{StaticResource ModernScrollBarThumb}"/>
                                                                </Track.Thumb>
                                                            </Track>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ScrollViewer.Resources>
                                    <StackPanel Name="VerticalTabPanel" Orientation="Vertical" 
                                               Background="Transparent" MinHeight="400"/>
                                </ScrollViewer>
                            </Border>
                            
                            <!-- 帳號列表區域 -->
                            <DataGrid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                     Name="AccountsDataGrid" AutoGenerateColumns="False" 
                              SelectionMode="Extended" CanUserAddRows="False" 
                              SelectionChanged="AccountsDataGrid_SelectionChanged"
                              Background="Transparent" BorderThickness="0"
                              GridLinesVisibility="None" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="分組" Binding="{Binding Account.Group}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="名稱" Binding="{Binding DisplayName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="帳號" Binding="{Binding DisplayUsername}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="密碼" Binding="{Binding DisplayPassword}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Secret Key" Binding="{Binding DisplaySecretKey}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource MutedBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>

            <!-- Account Details -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="帳號詳細資料" FontSize="18" FontWeight="SemiBold" 
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,16"/>

                    <TextBlock Grid.Row="1" Text="名稱" FontWeight="Medium" 
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,8,0,4"/>
                    <TextBox Grid.Row="2" Name="NameTextBox" Style="{StaticResource InputStyle}" 
                             IsReadOnly="True" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="3" Text="帳號" FontWeight="Medium" 
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,8,0,4"/>
                    <TextBox Grid.Row="4" Name="UsernameTextBox" Style="{StaticResource InputStyle}" 
                             IsReadOnly="True" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="5" Text="當前 TOTP" FontWeight="Medium" 
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,8,0,4"/>
                    <StackPanel Grid.Row="6" Orientation="Vertical" Margin="0,0,0,8">
                        <Border Background="{StaticResource MutedBrush}" CornerRadius="6" Padding="12,8" Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBox Name="TotpTextBox" Background="Transparent" BorderThickness="0" 
                                         IsReadOnly="True" FontFamily="Consolas" FontSize="24" FontWeight="Bold"
                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Name="TotpCountdownTextBlock" Margin="8,0,0,0" VerticalAlignment="Center"
                                           Foreground="{StaticResource MutedForegroundBrush}"/>
                            </StackPanel>
                        </Border>
                        <Button Name="CopyTotpButton" Content="複製 TOTP" Style="{StaticResource SecondaryButtonStyle}" 
                                Click="CopyTotpButton_Click" IsEnabled="False"/>
                    </StackPanel>

                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,8,0,0" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Name="StatusTextBlock" Text="就緒" 
                           Foreground="{StaticResource MutedForegroundBrush}" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="熱鍵: " Foreground="{StaticResource MutedForegroundBrush}"/>
                    <TextBlock Name="HotkeyTextBlock" Text="Home" FontWeight="SemiBold" 
                               Foreground="{StaticResource ForegroundBrush}"/>
                    <TextBlock Text=" | 帳號數量: " Foreground="{StaticResource MutedForegroundBrush}" Margin="8,0,0,0"/>
                    <TextBlock Name="AccountCountTextBlock" Text="0" FontWeight="SemiBold" 
                               Foreground="{StaticResource ForegroundBrush}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>